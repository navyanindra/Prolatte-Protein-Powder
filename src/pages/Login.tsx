// Deprecated experimental login page kept only for reference.
// The real app login lives in `pages/Login.tsx` and uses the global context.
// If you need to integrate API-based login, copy logic from here into that page.
export {};
